Yarn Stash - catalog craft fiber/textile supplies (yarn, fabric, thread) with a color swatch as the visual hero of each entry. Data persists in localStorage, no backend.

Run: `npm install && npm run dev`
Test: `npm test`
Build: `npm run build`
